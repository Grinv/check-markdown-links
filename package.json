{
  "name": "checkmdlinks",
  "version": "1.0.0",
  "description": "CLI checker for links in Markdown files (zero dependencies)",
  "type": "module",
  "bin": { "checkmdlinks": "./bin/checkmdlinks.ts" },
  "engines": { "node": ">=24.19.0 <25" },
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "coverage": "node --test --experimental-test-coverage"
  },
  "files": ["bin", "src", "README.md"],
  "license": "MIT",
  "private": true
}
